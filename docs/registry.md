# Model Registry

Registry for model descriptors with normalization of `:fields` and `:required`
and protection against duplicate registrations.

Related: migrations in `docs/migration.md`.

Module: `src/futon1a/model/registry.clj`.
