# Open-World Ingest

Open-world ingest with entity/relation validation and optional model registry
enforcement. When `require-model?` is true, entities must have registered
descriptors.

Module: `src/futon1a/ingest/open_world.clj`.
